<!DOCTYPE html>
<html>
<head>
	<title>Node world server</title>
	<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
</head>
<body>
  <div>
	<p>
		<form id="choosen_language_form" method="post" action="/">
  			<%= __('Language_choice')%> :
  			<select class="choosen_language" name="choosen_language">
				<% for(var i = 0; i < selectionnableLanguages.length; i++){ %>
					<option value="<%= selectionnableLanguages[i].value %>" <%= selectionnableLanguages[i].selected %>><%= __(selectionnableLanguages[i].text)%></option>
				<% } %>
  			</select>
		</form>
  		<h1><%= __('Hello')%></h1>
  	</p>
  </div>
  <div>
	<% for(var i = 0; i < timezones.length; i++){ %>
		<p style="color: <%= timezones[i].color %>"><%= timezones[i].timezone %> (<%= timezones[i].country_code %>): <%= timezones[i].timestamp %></p>
	<% } %>
  </div>
  <script>
	$("select").change(function(){

		$("#choosen_language_form").submit();
	});
  </script>
</body>
</html>
